
function findLongestWord(str) {
  var arr = str.split(/\s/);
   temp = arr[0].length;
  
  for(var i=1;i<arr.length;i++){
    if(arr[i].length>temp){
      temp = arr[i].length;
    }
  }
return temp;
}
findLongestWord("hello world！！！i'm pp！！");
